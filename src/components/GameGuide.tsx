// Trimmed: the previous Players / Roles tabs were removed.
// The moderator guide now lives in `./ModeratorGuide.tsx`.
// This file re-exports it for backward compatibility.
import ModeratorGuide from './ModeratorGuide';

export default ModeratorGuide;
